version: '3'

dotenv:
  - .env

tasks:
  init:
    cmds:
      - terraform init
  plan:
    cmds:
      - terraform plan
  apply:
    cmds:
      - terraform apply
  destroy:
    cmds:
      - terraform destroy